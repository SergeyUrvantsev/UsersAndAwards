@page "{id:Guid}"
@model UsersAndAwards.PL.WebApp.Pages.Awards.DeleteAwardModel
@{
	    ViewData["Title"] = "Delete award";
}


<h1>Delete</h1>

<h3>Are you sure you want to delete this?</h3>
<div>
    <h4>Award</h4>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Award.Title)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Award.Title)
        </dd>

        @foreach (var item in Model.Award.Users)
        {
            <dt class="col-sm-2">
                @Html.DisplayNameFor(modelItem => item.Name)
            </dt>
            <dd class="col-sm-10">
                @Html.DisplayFor(modelItem => item.Name)
            </dd>
        }
    </dl>

    <form method="post">
        <input type="hidden" asp-for="Award.Id" />
        <input type="submit" value="Delete" class="btn btn-danger" /> |
        <a asp-page="AwardsList">Back to List</a>
    </form>
</div>
